/*
 * Minimal zap-generated/access.h for Sesame.
 * All access control privilege overrides are handled by the default
 * (view privilege for reads, operate for writes/invokes) — no overrides needed.
 * privilege-storage.cpp guards all GENERATED_ACCESS_* usage with #ifdef.
 */
#pragma once
